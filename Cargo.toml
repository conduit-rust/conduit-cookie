[package]

name = "conduit-cookie"
authors = ["Alex Crichton <alex@alexcrichton.com>"]
version = "0.1.3"
license = "MIT"
description = "Cookie and session middleware for conduit-based stacks"
repository = "https://github.com/conduit-rust/conduit-cookie"

[dependencies]
cookie = "0.1.0"
conduit = "0.5.0"
conduit-middleware = "0.5.0"
rustc-serialize = "0.2"

[dev-dependencies]
conduit-test = "0.5.0"
